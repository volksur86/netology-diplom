[defaults]
inventory = ./hosts
remote_user = ubuntu
host_key_checking = False
timeout = 30
roles_path = ./roles

[ssh_connection]
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ForwardAgent=yes

